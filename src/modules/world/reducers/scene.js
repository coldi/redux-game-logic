const scene = (

    state = {
        cameraPosition: [2, 2, 5],
        cameraTarget: [2, 0, 0],
        lightPosition: [5, 5, 5],
    },
    action = {}

) => {

    return state

};

export default scene;